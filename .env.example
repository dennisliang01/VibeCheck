# Optional: use Claude for project map, questions, and grading.
# If unset or USE_CLAUDE_LLM is not "true", the app uses the mock LLM (no API key).
USE_CLAUDE_LLM=true
ANTHROPIC_API_KEY=sk-ant-your-key-here

# Optional: Claude model (default is claude-3-5-sonnet-20241022)
# ANTHROPIC_MODEL=claude-3-5-sonnet-20241022
